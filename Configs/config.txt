# configs to connect database
# host user pass databaseName port
database_info|localhost|root|my_db_password|my_database_name|3306|

# static server using while sending OnSuperMain
# cdnURL cdnPATH(SLASH REQUIRED)
cdn_server|127.0.0.1|cache/|

# world renderer assets(rttex, font) path NO SLASH AT END
renderer_static_path|

# world renderer save path for results NO SLASH AT END
renderer_save_path|

# game worlds save path NO SLASH AT END
world_save_path|

# X people can login at once others gonna kicked
max_logins_at_once|20

# only X accounts can be created
max_accounts_per_ip|5
max_accounts_per_gid|5
max_accounts_per_vid|5
max_accounts_per_sid|5
max_accounts_per_mac|5

# max player count in a world
world_max_player_count|70

# 0 for only accept default enet command
# 1 for only accept growtopia's enet command
# 3 for accept both
# used for growtopia's new enet protocol 
# type2 not working for newer clients
enet_income_cmd_type|3

# make 0 to disable, if disabled it gonna choose item data version range from X_min_version to X_max_version
# WARNING! auto choose from range is very experimental!

force_item_data_version|5

# SUPPORTING MINIMUM VERSION 3.02 BECAUSE OF ZLIB COMPRESS!!!!!
# need to find a solution for it to support older than 3.02
# there is a way but it gonna cause high memory usage

android_supported_versions|3.02|5.45
windows_supported_versions|3.02|5.45
ios_supported_versions|3.02|5.45
macos_supported_versions|3.02|5.45