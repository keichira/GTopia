# Ports (Port = Start Port + Server ID)
tcp_start|18500
udp_start|18000

# set_master|INTERNAL_LAN_IP|EXTERNAL_WAN_IP|
# add_server|INTERNAL_LAN_IP|EXTERNAL_WAN_IP|COUNT|
# add_renderer|INTERNAL_LAN_IP|EXTERNAL_WAN_IP|COUNT|

# VPS / VDS / PRODUCTION EXAMPLE:
#    If hosting on a virtual server
#    and WAN is your public Server VPS IP.
#    set_master|192.168.1.5|95.211.4.12|
#    add_server|192.168.1.5|95.211.4.12|1|

# Master Server configuration (Must be on top)
set_master|127.0.0.1|127.0.0.1|

# Registering sub-servers to Master
add_server|127.0.0.1|127.0.0.1|1|

# add_renderer|127.0.0.1|127.0.0.1|1|